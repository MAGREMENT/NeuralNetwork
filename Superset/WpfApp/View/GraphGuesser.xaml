<Page x:Class="WpfApp.View.GraphGuesser"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:WpfApp.View"
      mc:Ignorable="d"
      Title="GraphGuesser" Height="650" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <local:GuessingGraph x:Name="Graph" Grid.Column="0"/>
        
        <StackPanel Grid.Column="1" Orientation="Vertical" Margin="20, 0, 20, 0" x:Name="ControlPanel">
            <StackPanel Orientation="Horizontal">
                <Button Click="Generate">Generate</Button>
                <Button Click="Remove">Remove</Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock>Cost :</TextBlock>
                <TextBlock x:Name="CostBlock"></TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Click="Learn">Learn</Button>
                <Button Click="Stop">Stop</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
