<Page x:Class="WpfApp.View.DoodleGuesser"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:WpfApp.View"
      mc:Ignorable="d"
      Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Border Grid.Row="0" Grid.Column="0" BorderBrush="Black" BorderThickness="1" 
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Margin="20 0 20 0" Grid.RowSpan="2">
                <local:DoodleDrawer Width="300" Height="300" x:Name="Drawer"/>
            </Border>
            
            <Border Grid.Row="0" Grid.Column="1" Margin="0 20 20 10"
                    BorderBrush="Black" BorderThickness="1" Padding="10">
                <TextBlock FontSize="20">Expected : ?</TextBlock>
            </Border>
            
            <ScrollViewer Grid.Row="1" Grid.Column="1" Margin="0 0 20 0">
                <StackPanel x:Name="Predictions"/>
            </ScrollViewer>
        </Grid>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Height="50" Margin="10" Click="Previous">Previous</Button>
            <Button Grid.Column="1" Height="50" Margin="10" Click="Next">Next</Button>
        </Grid>
    </Grid>
</Page>
